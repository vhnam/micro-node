const express = require('express');
const router = express.Router();

// const sampleValidator = require('../validators/sample');
// const sampleController = require('../controllers/v1/sample');

// router.get('/samples', sampleValidator.getSamples, sampleController.getSamples);

module.exports = router;
